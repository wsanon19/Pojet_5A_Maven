spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/apishop
    username: postgres
    password: Motamu2000?
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate :
      ddl-auto: create
